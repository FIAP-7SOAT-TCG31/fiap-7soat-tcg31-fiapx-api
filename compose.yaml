version: "3.8"
services:
  localstack:
    image: localstack/localstack:latest
    ports:
      - "4566:4566" # Porta principal do LocalStack
      - "4572:4572" # Porta S3 (opcional)
    environment:
      - SERVICES=s3
      - AWS_DEFAULT_REGION=us-east-1